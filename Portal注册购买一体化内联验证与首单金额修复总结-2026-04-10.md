# Portal注册购买一体化内联验证与首单金额修复总结 - 2026-04-10

## 1. 项目背景

本轮工作的直接背景，是将未登录购买页稳定收敛为“注册 + 购买一体化”入口，而不是普通游客购买页。业务目标已经明确：

- 已存在且已验证账号用户，继续支持“邮箱 + 密码”直接购买。
- 新邮箱用户、已存在但未验证邮箱用户，不跳独立注册页，而是在 purchasing 页内联完成邮箱验证码验证后继续下单。
- 不采用“支付后再验证并激活账号”的路线。
- 不大改现有订单状态、支付回调、激活队列主模型。

在这个前提下，本轮后端先后完成了两类工作：

1. 增加 portal A+ 的内联验证码与 ticket 支撑能力。
2. 修复“新邮箱 portal 首单误吃 signup promo，导致订单金额错误”的建单问题。

相关提交：

- `a81d6b2` `feat(portal): add inline verification checkout flow`
- `b9167ed` `fix(portal): keep new-email first order amount stable`

## 2. 本轮后端改动内容

### 2.1 Portal A+ 基础支撑

新增和扩展了 portal 购买前置能力，使 purchasing 页可以在当前页内完成邮箱验证后继续下单：

- `POST /v1/public/portal/pre`
  返回账号模式、下一步动作、验证类型、是否需要密码。
- `POST /v1/public/portal/send_code`
  由 portal 侧统一判断账号类型并发送对应验证码，不再让前端自己猜 `register` 还是 `security`。
- `POST /v1/public/portal/verification/ticket`
  校验验证码后生成一次性 `portal_verification_ticket`，供 `portal purchase` 消费。
- `POST /v1/public/portal/purchase`
  支持三类用户分支：
  - 已存在已验证邮箱：密码直购
  - 新邮箱：ticket + password 后建号并下单
  - 已存在未验证邮箱：ticket + password 后验证并下单

同时增加了以下底层支撑：

- portal 专属错误码
- portal verification ticket 的 Redis 存储和一次性消费
- portal 发送验证码与下单的基础风控
- 同邮箱 pending portal 订单复用，避免重复脏单

### 2.2 新邮箱首单金额修复

在 A+ 支撑代码落地后，实际验收中确认了一个新问题：

- 新邮箱 portal 流程下，`/portal/pre` 预览金额正常。
- 但 `portal purchase` 在新邮箱分支中会先建号、发放 signup promo，再立即锁定并应用 promo。
- 结果导致当前这笔新邮箱 portal 首单直接吃掉 1000 分优惠，订单金额从应付 `2049` 变成了错误的 `1019`。

本轮修复后的策略是：

- 新邮箱 portal purchase 仍然可以在事务内建号。
- 仍然会发放 signup promo。
- 但当前这笔首单不再立即锁定和消费刚发放的 signup promo。
- 这样当前订单金额与 `/portal/pre` 预览保持一致，而 promo 保留给后续订单使用。

## 3. 关键接口变更

### 3.1 `POST /v1/public/portal/pre`

新增返回字段：

- `account_mode`
- `next_action`
- `verification_type`
- `require_password`

已有字段保留：

- `price`
- `amount`
- `fee_amount`
- `coupon_discount`
- `promo_discount`
- `can_purchase`
- `purchase_block_reason`

### 3.2 `POST /v1/public/portal/send_code`

本轮新增，用于 portal 购买页内联验证码发送。

主要职责：

- 识别账号类型
- 识别验证码类型
- portal 专属发送频控
- 新邮箱域名限制
- 调用现有验证码发送逻辑

### 3.3 `POST /v1/public/portal/verification/ticket`

本轮新增，用于生成 `portal_verification_ticket`。

主要职责：

- 校验验证码
- 判断账号模式
- 生成 ticket
- 把 ticket 写入 Redis

### 3.4 `POST /v1/public/portal/purchase`

新增请求字段：

- `portal_verification_ticket`

继续返回：

- `order_no`
- `payable_amount`

当前语义已经明确：

- `payable_amount` 就是当前订单的真实应付金额。

## 4. 三类用户流程支持情况

### 4.1 新邮箱

当前已支持：

1. `pre` 返回 `account_mode=new_email`
2. `next_action=verify_email`
3. `verification_type=register`
4. `send_code` 成功
5. `verification/ticket` 成功
6. `purchase` 成功建号并创建订单

修复后，当前这笔首单不会再误吃 signup promo。

### 4.2 已存在已验证邮箱

当前已支持：

1. `pre` 返回 `account_mode=existing_verified`
2. 未填密码时返回 `next_action=input_password`
3. 正确密码可以直接 `purchase`
4. 不需要 ticket
5. 同邮箱 pending order 复用仍然有效

### 4.3 已存在未验证邮箱

当前已支持：

1. `pre` 返回 `account_mode=existing_unverified`
2. `next_action=verify_email`
3. `verification_type=security`
4. `send_code` 成功
5. `verification/ticket` 成功
6. `purchase` 成功
7. 下单事务中将 `verified` 更新为 `true`

## 5. 关键问题修复

本轮实际修复的关键问题是：

### 新邮箱 portal 首单金额错误

修复前：

- `/portal/pre.amount = 2049`
- `/portal/purchase.payable_amount = 1019`
- DB `order.amount = 1019`
- checkout 支付金额 = `10.19`

修复后：

- `/portal/pre.amount = 2049`
- `/portal/purchase.payable_amount = 2049`
- DB `order.amount = 2049`
- checkout 支付金额 = `20.49`

结论：

- 问题已定位并修复在后端建单层。
- checkout 和 order status 没有再被误判为根因。

## 6. 验收结果

### 6.1 新邮箱主链路

实测通过：

- `pre` 正确返回 `new_email + verify_email + register`
- `send_code` 成功
- `verification/ticket` 成功
- `purchase` 成功
- 金额四处一致：
  - `pre.amount`
  - `purchase.payable_amount`
  - DB `order.amount`
  - checkout 第三方金额

额外确认：

- 新用户的 signup promo 仍然会被创建。
- 但当前首单不会绑定或消费这张 promo。

### 6.2 已存在已验证邮箱

实测通过：

- 正确密码可直接购买
- 金额保持正常
- 重复下单会复用 pending order，不重复造单

### 6.3 已存在未验证邮箱

实测通过：

- security 验证码链路可用
- ticket 可生成
- 下单金额正常
- `verified` 会在下单事务中转为 `true`

## 7. 当前未完成项

本轮范围内未处理但后续仍建议完善的内容：

- 还没有补自动化测试
- 还没有补 portal 专属的细粒度监控/日志埋点
- 新邮箱域名限制目前基于已有配置能力，暂未扩展为更完整的临时邮箱黑名单体系
- 本轮没有扩展 checkout / notify / activate 主流程，只保持现有主干不变

## 8. 后续建议

1. 先让前端按当前接口进入联调，重点联调：
   - 新邮箱内联验证后下单
   - 已验证邮箱密码直购
   - 未验证邮箱验证后下单

2. 补最少 3 组自动化测试：
   - 新邮箱 portal 金额一致性测试
   - 已验证账号 pending order 复用测试
   - 未验证账号验证后下单测试

3. 在联调稳定后，再评估是否补第二阶段风控：
   - 更强的 portal purchase IP 限流
   - 更完整的临时邮箱黑名单
   - 更细的 ticket 风险审计日志

4. 若后续继续演进 purchasing 页，仍建议坚持当前原则：
   - 不跳手动注册页
   - 不走支付后再验证激活
   - 不破坏现有订单与激活主模型
