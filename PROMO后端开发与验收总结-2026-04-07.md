# PROMO后端开发与验收总结 - 2026-04-07

## 1. 本次功能目标

本次后端整理和收口的目标，是把“新人首单优惠”相关能力完整接入现有购买与支付链路，并把 portal、公网下单、支付回调、订单状态流转、用户侧展示与关闭、API 契约、迁移脚本统一到同一套 promo 语义下，避免出现“预览有优惠、下单未占用、支付后未核销、取消后未释放、前端拿不到状态”的割裂情况。

本次 promo 规则以“注册成功后发放一次首单固定优惠，限时有效”为核心，重点覆盖三类场景：

- 新用户注册后自动发放 promo grant。
- 下单预览、正式下单、支付完成、订单关闭/取消时正确处理 promo 的预占、核销、释放。
- 用户中心和 portal 侧能够获取 promo 状态，并支持前端关闭提示弹窗。

## 2. 本次新增和修复的内容

### 2.1 Promo 领域能力

- 新增 `user_promo_grants` 数据表迁移脚本：`02135_user_promo_grants.up.sql` / `02135_user_promo_grants.down.sql`。
- 为 `order` 表补充 `promo_campaign_key`、`promo_discount` 字段，保证订单记录可追踪优惠来源与抵扣金额。
- 新增 `internal/model/userpromo` 模型层，统一管理用户 promo grant 数据。
- 新增 `internal/promo/service.go`，集中处理发放、预览、锁定、绑定订单、核销、释放、状态归一化等逻辑。

### 2.2 注册/登录后发放 promo

- 在邮箱注册、手机注册、OAuth 注册、设备登录自动注册等新建用户路径中，统一接入 `GrantSignupPromo`。
- portal 首次购买场景下，如果用户不存在，则在建号事务内补齐用户、认证方式、邀请码关联，并同步发放新人 promo。

### 2.3 下单与 portal 购买链路

- 公网普通购买、续费预览、portal 预购、portal 正式购买、portal 查询订单等链路补齐 promo 字段返回。
- 预下单时加入 promo 折扣预览，保证前端看到的金额与后端实际下单金额一致。
- 正式创建订单时，在事务内锁定 grant、记录 `promo_campaign_key` / `promo_discount`，并在订单写入后回填绑定关系。
- fee 计算顺序统一为“原价 -> 套餐折扣/优惠码 -> promo -> 手续费 -> 余额抵扣”，减少金额错位。

### 2.4 支付回调、订单关闭与后台改状态

- Alipay、EPay、Stripe 回调成功后，在同一事务内同时更新订单状态并核销 promo。
- balance 支付零金额/非零金额完成后，同样补上 promo 核销。
- 用户主动关闭订单、后台将订单改为取消/关闭时，释放 grant 预占，避免优惠被长期锁死。
- 关闭订单时顺手修正了库存回补和用户赠送金缓存更新时机，避免事务外状态不一致。

### 2.5 用户侧接口与 API 契约

- `QueryUserInfo` 返回新增 `promo` 字段，提供当前 promo 状态、剩余时间、是否已关闭提示等信息。
- 新增用户接口 `PUT /promo/dismiss`，支持前端关闭 promo 提示。
- `portal.api`、`user.api`、`types.api`、`internal/types/types.go` 对齐新增字段：
  - `promo_discount`
  - `promo_campaign_key`
  - `promo`
  - portal 预购时补充 `auth_type` / `identifier` / `password` 用于登录态识别与新注册预览

## 3. 涉及的关键模块

- Promo 核心服务：`internal/promo/service.go`
- Promo 数据模型：`internal/model/userpromo/`
- 数据迁移：`initialize/migrate/database/02135_user_promo_grants.*.sql`
- 用户注册/登录：`internal/logic/auth/`
- 公网订单：`internal/logic/public/order/`
- Portal 购买链路：`internal/logic/public/portal/`
- 支付回调：`internal/logic/notify/`
- 后台订单状态更新：`internal/logic/admin/order/updateOrderStatusLogic.go`
- 用户信息与用户操作接口：`internal/logic/public/user/`、`internal/handler/public/user/`
- API 契约与类型：`apis/`、`internal/types/types.go`
- 服务装配：`internal/svc/serviceContext.go`

## 4. 本次真实验证了哪些链路

本次实际完成的验证，以上游代码整理和静态自检为主：

- 已逐文件核对以下状态流转是否闭环：
  - 注册发放 promo
  - 预览金额包含 promo
  - 下单事务内锁定并绑定 promo
  - 支付成功核销 promo
  - 订单关闭/取消释放 promo
  - 用户信息接口返回 promo 状态
  - 用户关闭 promo 提示后状态可持久化
- 已补齐 API 契约和 `internal/types`，保证 portal / user / order 返回字段一致。
- 已执行 `git diff --check`，当前纳入范围的改动未发现空白符类问题。
- 当前环境缺少 Go 工具链，`go` 与 `gofmt` 均不在 PATH，因此本次未能执行 `gofmt` / `go test ./...` 编译级校验。

## 5. 还未完全验证的风险点

- 缺少真正跑通的端到端支付验收，尤其是第三方回调实际到达后的幂等性与重复通知行为。
- 当前机器没有 Go 工具链，无法在本次整理提交过程中完成本地编译与测试回归。
- portal 新用户首购场景依赖 `auth_type` / `identifier` / `password` 识别，前端若未按新契约传参，会影响 promo 预览准确性。
- `DismissUserPromo` 当前只处理单一 campaign，后续若扩展多活动，需要补用户侧展示优先级与多活动策略。
- promo 过期与已支付订单判断依赖运行时查询订单状态，目前没有单独的集成测试覆盖边界时序。
- 当前工作区存在本地 Docker 与配置改动，但这些不属于本次代码提交；若部署环境未同步迁移，运行时仍可能出现“代码已引用 promo 字段但数据库未升级”的问题。

## 6. Docker 重建与迁移生效情况

- 代码层面：本次已经补齐 promo 所需数据库迁移脚本，并在订单模型中接入新字段。
- 提交范围层面：当前工作区存在 `docker-compose.yml`、`etc/ppanel.yaml`、`etc/ppanel.yaml.bak`、`data/` 等本地部署/运行文件改动，这些不纳入本次 promo 提交。
- 生效状态层面：本次任务目标是整理并提交 promo 后端代码，不包含重新打包部署；因此本次提交只保证“代码与迁移已就绪”，不宣称已经完成线上/容器环境重建。
- 部署时需要明确执行：
  - 重新构建并启动包含本次代码的服务镜像/容器
  - 执行 `02135_user_promo_grants` 迁移
  - 确认 `order` 表新增字段已生效后再做支付链路回归

## 7. 最近一段时间已经提交过的相关 git 记录摘要

结合最近若干条 `git log`，与本次 promo 后端收口直接相关的既有工作大致如下：

- `01e6e7c`：portal 公网购买金额里已补上 payment fee 计算，说明 portal 金额计算链路最近已经做过一次修正，本次是在这个基础上继续把 promo 折扣纳入统一金额口径。
- `3bb0f72`：设备认证异常隔离，避免设备配置问题影响普通认证流程；这对“设备登录自动注册并发放 promo”这次接入是有帮助的底座修复。
- `7197f5d` / `ea94f3c`：Stripe webhook 构造兼容性已修正，说明支付通知链路此前已经做过稳定性处理，本次只是在成功回调后补上 promo 核销。
- `701d47e`：订单数量与金额上限校验已经补齐；本次 promo 金额计算仍沿用该安全边界，没有绕开已有限制。
- `7e08a07`：余额支付与 coupon / fee 计算问题曾经修正过；本次是把 promo 折扣纳入同一金额计算顺序，继续延伸这条支付金额一致性修复。
- `34372fe`：兑换码流程已经强化了事务安全和幂等处理；本次 promo 设计同样采用事务内锁定、绑定、核销、释放，风格上与既有支付/权益发放治理方向一致。
- `5f1a546`：新用户注册后的 trial/cache 问题曾经处理过，说明注册后立即发权益并同步状态并不是首次出现；本次 promo 发放沿用了这类“注册即派发”的思路。
- `7fe7243` 以及其前后的 captcha / auth 一组提交：最近登录、注册、验证码、防刷这部分已经持续完善，本次把 promo 发放接在这些登录注册链路后面，属于在既有认证基础上的增量接入，而不是重做认证体系。

整体上看，最近一段时间已经把“认证更稳、portal 金额更准、支付通知更稳、订单金额校验更严”这些基础工作铺得比较完整；本次新增 promo，更多是在这些现有稳定链路上补齐优惠状态机和 API 契约。

## 8. 当前建议的下一步动作

1. 先把本次 promo 相关代码和总结文档独立提交，避免和本地 Docker/配置改动混在一起。
2. 在测试环境执行数据库迁移后，至少回归 4 条真实链路：
   - 新注册用户首购预览金额
   - 新注册用户首购下单并支付成功
   - 下单后主动取消/关闭订单再重新下单
   - 用户关闭 promo 弹窗后重新获取用户信息
3. 追加最少一组 promo 服务单元测试或集成测试，覆盖“过期、已用、已预占、支付成功、订单关闭释放”几个边界状态。
4. 如果后续还会有第二类活动，尽快把 `campaign_key` 扩展策略、前端展示优先级、用户多活动状态聚合规则设计出来。
