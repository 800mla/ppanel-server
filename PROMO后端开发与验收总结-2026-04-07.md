# PROMO后端开发与验收总结 - 2026-04-07

## 1. 本次功能目标

本次后端工作的目标，不是重新设计支付或订单体系，而是在现有购买链路上补齐“新人首单优惠”的完整后端闭环，使 promo 能够以统一语义进入注册、portal、公网下单、支付通知、订单关闭、用户信息展示和 API 契约。

本次目标聚焦四件事：

- 新用户注册成功后自动获得一份可追踪、可过期、可核销的首单 promo grant。
- 购买预览和正式下单金额口径一致，避免出现“预览有优惠、落单没优惠”或“支付金额与查询金额不一致”。
- 支付成功、后台改单、用户取消订单时，promo 能正确完成核销或释放，不留下脏占用。
- 前端能从用户信息和 portal 订单接口里拿到 promo 状态，并可关闭提示弹窗。

本次 promo 规则是固定金额的新人首单限时优惠，底层需要解决的不只是折扣计算，还包括 grant 生命周期、订单绑定关系和支付后状态流转。

## 2. 修改过的核心文件分组

本次改动涉及 36 个文件，但职责边界比较清晰，可以按以下几组理解。

### 2.1 Promo 数据模型与服务层

这部分是本次新增能力的核心，负责定义 promo 的数据存储、状态机和事务内操作。

- `initialize/migrate/database/02135_user_promo_grants.up.sql`
- `initialize/migrate/database/02135_user_promo_grants.down.sql`
- `internal/model/userpromo/default.go`
- `internal/model/userpromo/model.go`
- `internal/model/userpromo/userpromo.go`
- `internal/promo/service.go`
- `internal/svc/serviceContext.go`

这一组主要完成：

- 新增 `user_promo_grants` 表，保存用户级 promo 发放记录。
- 为 `order` 表补充 `promo_campaign_key`、`promo_discount` 字段，用于订单侧可追踪落库。
- 在服务层统一封装 `GrantSignupPromo`、`PreviewDiscountForUser`、`LockGrantForOrder`、`BindGrantToOrder`、`ConsumePromoByOrderID`、`ReleaseReservationByOrderID`。
- 把“待激活、已激活、已关闭提示、已使用、已过期”这些状态归一化到同一处处理，避免在多个逻辑文件里各写一套判断。

### 2.2 注册、登录与 portal 建号接入

这部分负责把 promo 发放动作接到真实用户创建路径上。

- `internal/logic/auth/deviceLoginLogic.go`
- `internal/logic/auth/oauth/oAuthLoginGetTokenLogic.go`
- `internal/logic/auth/telephoneUserRegisterLogic.go`
- `internal/logic/auth/userRegisterLogic.go`
- `internal/logic/public/portal/orderUser.go`

这一组主要完成：

- 邮箱注册、手机注册、OAuth 注册、设备登录自动建号后，统一发放新人 promo。
- portal 首购场景补齐“识别已有用户 / 自动创建用户 / 建认证方式 / 关联邀请码”的事务流程。
- portal 新建用户时同步发放 promo，避免 portal 建号路径漏发新人优惠。

### 2.3 公网订单、portal 下单与金额计算

这部分负责把 promo 纳入订单创建和金额计算流程。

- `internal/logic/public/order/preCreateOrderLogic.go`
- `internal/logic/public/order/purchaseLogic.go`
- `internal/logic/public/order/renewalLogic.go`
- `internal/logic/public/portal/prePurchaseOrderLogic.go`
- `internal/logic/public/portal/purchaseLogic.go`
- `internal/logic/public/portal/purchaseCheckoutLogic.go`
- `internal/logic/public/portal/queryPurchaseOrderLogic.go`
- `internal/model/order/default.go`
- `internal/model/order/model.go`
- `internal/model/order/order.go`

这一组主要完成：

- 预下单时提前返回 `promo_discount`，让前端金额预览与正式订单保持一致。
- 正式下单时在事务内锁定 grant，并将 promo 信息写入订单。
- 统一金额顺序为“套餐优惠 / coupon -> promo -> fee -> 余额抵扣”。
- portal 首购场景下同时处理“未注册用户首购”和“已注册用户首购预览”。
- 查询订单时返回 `promo_campaign_key`、`promo_discount`，便于 portal 侧展示和排查。

### 2.4 支付通知、订单关闭和后台订单状态流转

这部分负责处理 promo 的核销和释放。

- `internal/logic/notify/alipayNotifyLogic.go`
- `internal/logic/notify/ePayNotifyLogic.go`
- `internal/logic/notify/stripeNotifyLogic.go`
- `internal/logic/public/order/closeOrderLogic.go`
- `internal/logic/admin/order/updateOrderStatusLogic.go`

这一组主要完成：

- 第三方支付成功后，在同一事务内更新订单状态并核销 promo。
- 余额支付链路补齐零金额和非零金额订单的 promo 核销。
- 用户关闭订单、后台取消订单或关闭订单时释放 promo 预占。
- 关闭订单时顺带修正库存回补和赠送金缓存更新顺序，降低订单关闭后的状态不一致风险。

### 2.5 用户接口、路由和 API 契约

这部分负责对外可见的契约变更。

- `apis/public/portal.api`
- `apis/public/user.api`
- `apis/types.api`
- `internal/types/types.go`
- `internal/handler/routes.go`
- `internal/handler/public/user/dismissUserPromoHandler.go`
- `internal/logic/public/user/dismissUserPromoLogic.go`
- `internal/logic/public/user/queryUserInfoLogic.go`

这一组主要完成：

- 用户信息接口新增 `promo` 状态块。
- 新增 `PUT /promo/dismiss`，允许前端关闭 promo 提示。
- portal 预购请求补充 `auth_type`、`identifier`、`password`，用于识别已有用户和新用户预览。
- 订单与预览返回补充 `promo_discount`、`promo_campaign_key` 等字段，保证前后端契约一致。

## 3. 每一轮关键修复做了什么

这次 promo 后端整理并不是一次单点改动，而是逐轮把链路从“能算优惠”推进到“状态闭环可追踪”。

### 第一轮：建立 promo 存储和状态语义

先落地迁移和模型层，明确 promo grant 作为用户级对象存在，而不是把优惠临时塞在下单逻辑里。这样后续才能支持过期、关闭提示、已使用、重新释放等状态。

这一轮的关键成果是：

- 新增 `user_promo_grants` 表。
- 给订单补充 promo 字段。
- 把 promo 生命周期收敛到 `internal/promo/service.go`。

### 第二轮：把 promo 接入注册与 portal 建号

如果只有订单侧会算 promo，但注册时没有发放记录，整个链路就不成立。因此第二轮把所有建号入口统一接入发放。

这一轮的关键成果是：

- 常规注册、手机注册、OAuth、设备登录自动建号统一发放新人 promo。
- portal 下单时支持“边建号边下单”，不再要求用户必须先走独立注册流程。

### 第三轮：修正金额计算顺序，避免预览与落单不一致

这轮重点处理金额口径。promo 必须进入正式订单金额，而不是只出现在前端展示里。

这一轮的关键成果是：

- 预下单和正式下单都纳入 promo 计算。
- promo 抵扣发生在 fee 之前，保证手续费按真实支付前金额计算。
- 订单写库时保存 `promo_campaign_key` 和 `promo_discount`，为后续核销和排查留痕。

### 第四轮：补齐支付成功、订单关闭、后台改状态的状态机

这轮解决的是“优惠会不会被锁死、会不会重复使用”的问题。

这一轮的关键成果是：

- 支付成功后同事务核销 promo，避免订单已支付但 grant 仍停留在可用状态。
- 用户取消、后台关闭、后台取消时释放 grant 预占，允许后续重新下单。
- 对已支付和已关闭订单做条件判断，降低重复通知和重复处理带来的状态错乱。

### 第五轮：补齐用户侧可见状态和关闭提示能力

这轮解决的是前端可见性和用户体验问题。

这一轮的关键成果是：

- `QueryUserInfo` 返回 promo 状态、到期时间、剩余秒数、是否 dismiss。
- 新增用户关闭 promo 提示接口。
- portal 订单查询和预览接口补齐 promo 字段。

### 第六轮：提交边界收口与变更记录整理

最后一轮不是再写功能，而是把本次 promo 相关文件与本地环境变更切开，保证提交边界干净、可回溯。

这一轮的关键成果是：

- 只提交 promo / portal / notify / API 契约 / 验收文档相关文件。
- 明确排除 `docker-compose.yml`、`etc/ppanel.yaml`、`.codex`、`backups/`、`data/` 等本地环境内容。
- 形成当前这份正式变更记录，方便后续验收和部署回顾。

## 4. Docker 重建与迁移生效情况

这部分需要区分“代码是否准备好”和“部署是否已经完成”。

### 4.1 已完成的代码准备

- promo 所需数据库迁移脚本已经补齐。
- `order` 模型与返回类型已经接入 promo 字段。
- 代码层已经假设迁移生效后可直接运行 promo 相关逻辑。

### 4.2 当前未纳入本次提交的本地环境文件

当前工作区存在以下本地环境改动，但它们不属于本次 promo 代码交付范围，也没有被纳入提交：

- `docker-compose.yml`
- `etc/ppanel.yaml`
- `etc/ppanel.yaml.bak`
- `.codex`
- `backups/`
- `data/`

### 4.3 生效状态结论

截至本次整理提交时，可以确认的是：

- 代码与迁移已经就绪。
- Docker 重建和迁移执行本身不在这次提交动作里。
- 因此当前不能宣称“容器已重建且迁移已现场验证生效”，只能确认“部署所需代码和脚本已经准备完毕”。

后续上线时必须显式执行：

1. 重新构建并启动包含本次代码的服务。
2. 执行 `02135_user_promo_grants` 迁移。
3. 确认 `order` 表新增字段与 `user_promo_grants` 表都已生效。
4. 再做支付回调和订单关闭回归。

## 5. 已真实验证通过的链路

本次能够明确确认通过的内容，主要是代码级链路闭环和提交边界检查；没有额外伪造“已线上验证”的结论。

### 5.1 已确认通过的代码级链路

- 注册成功后发放 promo 的接入点已经覆盖邮箱注册、手机注册、OAuth、设备登录自动建号、portal 自动建号。
- 预下单返回金额时已经包含 promo 折扣。
- 正式下单已在事务内锁定 grant、创建订单并绑定 grant。
- 支付成功后已补齐 promo 核销。
- 订单关闭、后台取消、后台关闭时已补齐 promo 释放。
- 用户信息接口已可返回 promo 状态。
- 用户关闭 promo 提示接口已经接入路由并落到逻辑层。
- `git diff --check` 已通过，本次纳入提交范围的改动没有空白符类问题。

### 5.2 当前未完成的运行态验证

当前环境缺少 Go 工具链，`go` 和 `gofmt` 都不在 PATH，因此本次无法补跑：

- `gofmt`
- `go test ./...`
- 基于本地编译的集成回归

同时，本次也没有在真实支付环境里重新走完整个在线支付回调验收。因此“已真实验证通过”的范围仅限于代码链路整理与静态自检，不包括线上支付实跑。

## 6. 最终剩余风险

当前剩余风险主要集中在运行态和边界时序，而不是功能缺口。

- 第三方回调虽然已经补了事务内核销，但仍缺少真实环境下的重复通知回归。
- portal 新用户首购预览依赖 `auth_type`、`identifier`、`password` 三个字段，前端如果未按新契约传参，预览金额可能与实际首购身份判断不一致。
- `DismissUserPromo` 目前按单活动设计，后续如果扩展多活动，需要补前端展示优先级和聚合策略。
- promo 过期、已支付订单判断依赖运行时订单状态查询，目前缺少专门的状态边界测试。
- 数据库若未执行迁移而先部署代码，运行时会直接出现字段或表缺失问题。

## 7. 最近相关 git 提交摘要

结合最近一段时间的 `git log`，当前 promo 收口并不是孤立工作，而是接在前几轮稳定性修复之后继续推进的。

- `01e6e7c fix(portal): include payment fee in public purchase total`
  之前已经把 portal 公网购买的 fee 计算口径修正过，本次是在同一金额链路里继续加入 promo 折扣。

- `3bb0f72 fix(device-auth): isolate invalid device config from common auth flows`
  之前已经把设备认证异常从公共登录链路里隔离开，本次设备登录自动建号发 promo 依赖这类认证稳定性修复。

- `7197f5d` / `ea94f3c feat(stripe): update webhook event construction to ignore API version mismatches`
  之前已经处理过 Stripe webhook 兼容性问题，本次是在支付成功分支继续补充 promo 核销，而不是重写整个 notify 流程。

- `701d47e feat(order): add validation for quantity and amount limits in order requests`
  之前已经补齐订单金额边界和数量校验，本次 promo 只是在既有限额保护内调整金额计算顺序。

- `7e08a07 fix: resolve balance payment issue with coupon and fee calculation`
  之前已经修过 coupon、fee、余额支付之间的金额关系，本次把 promo 纳入同一序列，是对既有金额一致性工作的继续完善。

- `34372fe fix(redemption): enhance redemption code flow with transaction safety and idempotency`
  之前已经在权益类流程里强调事务安全和幂等，本次 promo 的锁定、绑定、核销、释放采用了同样的治理思路。

- `5f1a546 fix: resolve trial subscription cache issue on new user registration`
  之前已经有“注册后立即发放权益并同步状态”的修复经验，本次 promo 发放沿用了类似模式。

整体来看，近期相关工作已经把认证、portal 金额、支付通知和订单金额边界逐步打稳；本次 promo 是在这些基础上补完优惠状态机、订单落库字段和用户可见状态。

## 8. 本次新增提交的内容概览

本次新增提交是：

- `1b57ea8 feat(promo): integrate first-order promo across portal flows`

这次提交的实际内容，可以概括为四块：

- 新增 promo 领域模型、迁移和服务层，使新人首单优惠成为可追踪的后端对象。
- 将 promo 接入注册、portal 建号、公网下单、portal 预览与正式购买。
- 将 promo 接入支付成功核销、订单关闭释放、后台订单状态更新。
- 补齐用户接口、portal 接口和 API 契约，并补了一份完整的后端开发与验收总结文档。

从交付边界看，这个提交已经覆盖了本次 promo 后端改动的主体代码；后续如果再补提交，应该只限于文档完善或验证性脚本，避免重新把无关环境文件混入。

## 9. 当前建议的下一步动作

1. 先将当前分支安全推送到 `origin`，保持本次 promo 代码与文档在远端可追踪。
2. 在具备 Go 工具链的环境补跑 `gofmt` 与 `go test ./...`。
3. 在测试或预发布环境执行数据库迁移后，至少回归以下链路：
   - 新注册用户首购预览金额
   - 新注册用户首购下单并支付成功
   - 下单后取消订单再重新下单
   - 用户关闭 promo 弹窗后重新获取用户信息
4. 在真实支付环境回归 Alipay、EPay、Stripe 至少各一条成功通知，确认核销幂等表现。
